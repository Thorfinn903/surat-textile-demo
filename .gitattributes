# Exclude non-code documentation from language statistics
docs/** linguist-documentation
*.md linguist-documentation

# Treat database migrations as generated code (Good practice)
migrations/** linguist-generated

# Note: We are NO LONGER excluding templates and static assets 
# so that HTML, CSS, and JS usage is visible in the stats.
